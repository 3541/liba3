[constants]
arch = 'x86_64-w64-mingw32-'

[binaries]
c = arch + 'gcc'
cpp = arch + 'g++'
strip = arch + 'strip'
ar = arch + 'gcc-ar'

[host_machine]
system = 'windows'
cpu_family = 'x86_64'
cpu = 'x86_64'
endian = 'little'
