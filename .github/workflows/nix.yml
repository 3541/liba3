name: Build and test with Nix

on:
  workflow_dispatch:
  push:
    paths:
      - '**/*.c'
      - '**/*.h'
      - '**/*.cc'
      - '**/*.hh'
      - '.github/workflows/nix.yml'
      - '**/meson.build'
      - 'boilerplate'
      - '**/*.nix'

jobs:
  build-nix:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [release, clang/release, mingw/release]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: DeterminateSystems/nix-installer-action@v5
      - name: Build with Nix
        run: nix build ".?submodules=1#a3/${{ matrix.target }}"
